<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.goldenasia.lottery"
    android:installLocation="auto"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:targetSdkVersion="23" />
    <!-- 完全退出使用的权限 -->
    <uses-permission android:name="android.permission.RESTART_PACKAGES" />
    <!-- 检查网络状态的权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 使用网络的权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SD卡读写 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- wifi 使用权限 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 手机状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 日志读取 -->
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 读取联系人 -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.Manifest.permission.STATUS_BAR" />
    <uses-permission android:name="android.Manifest.permission.EXPAND_STATUS_BAR" />
    <uses-permission android:name="android.Manifest.permission.STATUS_BAR_SERVICE" />

    <!-- 允许一个程序获取信息有关当前或最近运行的任务，一个缩略的任务状态，是否活动等等 -->
    <uses-permission android:name="android.permission.GET_TASKS" />
    <!-- 允许使用PowerManager的 WakeLocks保持进程在休眠时从屏幕消失 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_USER_PRESENT" />
    <!-- 允许挂载和反挂载文件系统可移动存储 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 允许一个程序打开窗口使用 TYPE_SYSTEM_ALERT，显示在其他所有程序的顶层 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!--<instrumentation android:name="android.test.InstrumentationTestRunner"-->
    <!--android:targetPackage="com.goldenasia.lottery" />-->

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:name=".app.GoldenAsiaApp"
        android:theme="@style/AppTheme.NoActionBar"
        android:label="@string/app_name">
        <uses-library android:name="android.test.runner" />


        <activity
            android:name=".app.DebugActivity"
            android:label="测试"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="keyboardHidden|orientation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen"
            android:windowSoftInputMode="adjustPan"
            android:screenOrientation="portrait">
            <!-- android:windowSoftInputMode="stateVisible|adjustResize" -->
            <!-- android:screenOrientation="portrait" android:windowSoftInputMode="adjustPan" -->
            <!--<intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <!-- <activity android:name=".view.AppendBetting" android:theme="@android:style/Animation.Activity" /> -->
        <activity android:name=".view.Tab01Activity" />
        <activity android:name=".view.Tab02Activity" />
        <activity android:name=".view.Tab03Activity" />
        <activity android:name=".view.Tab04Activity" />
        <activity android:name=".view.Tab05Activity" />

        <activity android:name=".app.FragmentLauncher"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="keyboardHidden|orientation"
            android:screenOrientation="portrait"/>


    </application>

</manifest>